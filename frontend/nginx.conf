server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Flutter web is an SPA: serve index.html for unknown routes.
  location / {
    try_files $uri $uri/ /index.html;
  }
}

