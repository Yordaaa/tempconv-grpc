apiVersion: v1
kind: Service
metadata:
  name: tempconv-frontend
  labels:
    app: tempconv-frontend
spec:
  type: LoadBalancer
  selector:
    app: tempconv-frontend
  ports:
    - name: http
      port: 80
      targetPort: http

